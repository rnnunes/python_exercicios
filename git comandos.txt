subir pelo git bash ou ir verificando pelo vs code

git status:
verificar

git add .
antes de dar o commit

git commit =m "mensagem"


git log:
historico

git push origin main
enviar para o github diretamente para a main

git pull:
puchar para do github para o computer.


no git bash já na pasta

para clonar um repositorio do github:
git clone + url do repositorio

para inicializar um repositorio a partir de uma pasta no computer:

git init

git remote add origin + url repositorio github / quando for criada a pasta na maquina e tiver de subir

git push origin main 
para enviar da pasta local para o github